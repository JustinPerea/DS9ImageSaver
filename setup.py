from setuptools import setup, find_packages

setup(
    name="ds9_region_saver",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here, if any
    ],
    entry_points={
        'console_scripts': [
            # Optionally add command-line scripts here
        ],
    },
)